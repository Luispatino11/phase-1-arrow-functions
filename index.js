const divide = function(num1, num2) {
    return num1 / num2;
  }
  
  console.log(divide(10, 2));
  
  const square = (num) => {
    return num * num;
  }
  
  console.log(square(5));
  
  const add = (num1, num2) => {
    return num1 + num2;
  }
  
  console.log(add(5, 10));
  